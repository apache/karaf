h1. Synopsis

${project.name}

${project.description}

Maven URL:
   [mvn:${project.groupId}/${project.artifactId}/${project.version}]

h1. Description

This bundle provides the shell commands to interact with the Karaf system container.

The following commands are available:
* system:shutdown - Shuts Karaf down.

h1. See also

Commands and Using the console sections of the Karaf User Guide
