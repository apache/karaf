h1. Synopsis

${project.name}

${project.description}

Maven URL
[mvn:${pct.groupId}/${project.artifactId}/${project.version}]

h1. Description

This bundle provides Karaf shell commands to manipulate web application (war) bundles.

The following commands are available:
* web:list - Lists war bundles
* web:deploy - Deploys a war bundle
* web:undeploy - Un-deploys a war bundles
