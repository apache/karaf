h1. Synopsis

${project.name}

${project.description}

Maven URL
[mvn:${pct.groupId}/${project.artifactId}/${project.version}]

h1. Description

This bundle provides Karaf shell commands to manipulate the PackageAdmin service.

It allows you to get export/import packages present in the OSGi framework.

The following commands available:
* packages:exports - Display exported packages
* packages:imports - Displays imported packages

h1. See also

Commands - section of the Karaf User guide
