h1. Synopsis

${project.name}

${project.description}

Maven URL:
[mvn:${project.groupId}/${project.artifactId}/${project.version}]

h1. Description

This bundle provides the Karaf features plugin for the Felix Web Console.

h1. See also

Web console - section of the Karaf User Guide
