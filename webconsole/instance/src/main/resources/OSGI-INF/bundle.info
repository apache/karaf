h1. Synopsis

${project.name}

${project.description}

Maven URL:
[mvn:${project.groupId}/${project.artifactId}/${project.version}]

h1. Description

This bundle provides usage of instance commands (to manipulate Karaf instances) in the
Karaf Web Console.

h1. See also

Web console - section in the Karaf User Guide
